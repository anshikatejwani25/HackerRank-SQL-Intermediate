WITH RECURSIVE numbers AS (
    SELECT 2 AS n
    UNION ALL
    SELECT n + 1 FROM numbers WHERE n + 1 <= 1000
)
SELECT GROUP_CONCAT(n SEPARATOR '&') AS primes
FROM numbers
WHERE n NOT IN (
    SELECT a.n
    FROM numbers a
    JOIN numbers b 
      ON a.n % b.n = 0 AND b.n < a.n
);
